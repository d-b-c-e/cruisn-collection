# Private page image proof

Run `python verify.py`. It checks source/receipt hashes and recomputes CPU timing
summaries from the sanitized benchmark CSV. Raw texture bytes, queued packets,
native binaries and GPU results remain local; their receipt verdicts are not
independently recomputed here. This is standalone ownership evidence, not live
material residency, GPU readiness, drawing distance or deployment acceptance.
