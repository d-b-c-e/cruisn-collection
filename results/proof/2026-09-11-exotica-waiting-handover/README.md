# Exotica waiting completion evidence

Run `python results/proof/2026-09-11-exotica-waiting-handover/verify.py`.
The verifier checks current helper source hashes and aggregate receipt consistency.
Actual game execution, lifecycle events, original geometry and GPU pixels remain
local evidence; the verifier does not replay or reconstruct those private files.

The native9ad observation preserves the full Amazon route and21 completed4K
images. Five waiting cohorts exclude ten original admissions at5072; the other
four cohorts are unchanged. A separately compiled, standalone helper reproduces
all30 independently checked ordered geometry variants. The full local check
receipt is390Python/no skips,51native tests and144commands includingGPU.

Offline split drawing preserves the existing future insertion and adds retained
waiting geometry at the observed command fence. Its sample still shows extra
distant trees, but color depends on ordering even when completed depth matches.
There is no new live waiting draw, fade/material/temporal acceptance, performance
claim or renewed seven-gameplay-default suite. Personal87d/v0.5.0 is unchanged.
