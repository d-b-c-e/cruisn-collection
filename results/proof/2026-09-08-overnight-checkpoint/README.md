# Final overnight checkpoint — September 8, 2026

- `menu-check.json` binds three offscreen menu previews to their source and saved
  config. Images were visually inspected. These show personal preferences, not
  fresh-install defaults or physical navigation acceptance.
- `ci.json` records all four passing jobs at `8157c3a`; the two source-identity
  artifacts match every one of the 268 local inputs. Subsequent edits only update
  documentation/evidence and do not change that product identity.
- `deployment.json` records the verified native executable, personal config,
  Stream Deck launch script and preserved v0.4.0 ZIP/tag hashes.
- `automation.json` records the scheduled 08:00 pause. No emulator/build remained
  running and no physical force was enabled during overnight automation.

The [morning handoff](../../../docs/OVERNIGHT-RESULTS-2026-09-08.md) separates
available source features from diagnostic trials and remaining attended checks.
No additional release was published.
