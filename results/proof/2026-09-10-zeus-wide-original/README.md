# Zeus wider-original diagnostic

Run `python verify.py` here. It recomputes source identity and the three complete
frame/counter tables, checks snapshot/count repeat identity, and verifies hashes
of execution receipts. Raw GPU buffers, geometry, palettes and textures stay local.
Their equality and actual replay/GPU/build execution remain hash-bound receipts.

All displayed output stays original. Private wide mode changes depth semantics;
its readback integrity is explicitly separate from per-pixel policy verification.
The seven sampled private color buffers equal original color. This adds no future
scenery, handover, performance, default-suite or deployment acceptance. A bounded
consumer-command journal is next: old captures only saved texture memory at end.
Personal v0.5.0 is unchanged; the prior USA timing failure remains unresolved.
