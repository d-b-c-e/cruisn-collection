# Actual worker-window measurements

Run `python results/proof/2026-09-11-ffb-worker-windows/verify.py` to verify current
source hashes and compact aggregate consistency. These receipts bind conservative
host/game clock joins and time-weighted output measurements on the four previously
accepted native936840 worker captures. They also bind the unchanged deterministic
input fields against the earlier condition analysis.

Only the centered, steady-input bin has two seconds in each game after uncertain
edges are excluded. Turn/contact coverage remains insufficient. The small Exotica
negative-medium sample often lies near its effective ceiling despite never
reaching that exact integer value; this is not a gain recommendation or proof of
physical clipping.

The public command does not read private motor traces, rebuild or execute MAME,
rerun the worker verifier or test a wheel. Execution and window computations are
receipts. No contact review or normalization acceptance is claimed.

LOCAL evidence is in `ffb-worker-condition-windows-ceiling` under the Amazon
diagnostic directory. The initial whole-frame-CSV hash rejection and intermediate
window reports are retained. See [the method and recording needs](../../../docs/reviews/2026-09-11-ffb-worker-windows.md).
