# Reject unexpected completed Zeus display dimensions

Run `python verify.py`. This exercises the archived validator against dimensions
from the retained wrong-size capture and two completed4K controls. It recomputes
source identity and checks the337-test Python receipt. It does not rerun the game,
read pixels, or renew the previous native/GPU/default suite. Only the three listed
harness/test files changed from the live-mirror checkpoint. Native25228 and the
personal installation are unchanged.
