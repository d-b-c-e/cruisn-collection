# Exotica extended-target presentation

Run `python verify.py`. Recomputes source identity and selected scalar/hash
relationships from four live full Amazon run receipts. Raw game assets, packets
and pixels stay local; actual route/GPU/4K/native-build checks remain receipts.

Original presentation preserves117old overlapping images; all four runs complete
233actual4K/CRT captures and preserve original input/camera/ADC and ten resources.
The source switch selects the final extended target explicitly; it is not a
normal launcher default. Independent sampled insertions match before/after.

Dense sampling finds33different3x-over2x frames that the14internal snapshots missed.
The largest differences occur at the grid; later samples show small changes through
foliage or at the left margin. Differences are not automatically visual gains.
No whole-track pop-in elimination, handover, performance or default-suite renewal
is claimed. Personal87d/v0.5.0 remains unchanged; no release/deployment.
