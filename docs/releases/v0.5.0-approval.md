# v0.5.0 release authorization — 2026-09-08

The maintainer watched the live Cheats tests and instructed this project session:

> I accept your testing that I'm currently watching for the Cheats menu fix. When done, please continue to upload a new release

This authorizes finishing the checks and publishing the current alpha to the
existing repository. It also follows the maintainer's earlier acceptance of the
v0.4.0 baseline and explicit deferral of World force normalization. It does not
authorize changing repository visibility or rewriting the previous tag/ZIP.

The current native candidate is `4ac6a84b51b4ae549399c81ffe1b9346e2c04758`, SHA256
`87d04de42d10a731f1d951a1fa378d784059d862063b224764ca6294fa5fe9d8`.
The final manifest and acceptance ledger bind the approval to the exact package,
native binary and source inputs. No rebuild is allowed during ZIP promotion.

## Coverage limits

The user's acceptance of the visible Cheats testing does not create new physical
wheel observations. Preserve the existing alpha limitations in the release notes
and enumerate unperformed human checks as waivers in the ledger, each with its
remaining coverage described. Do not label them observed passes or reuse stale
automated evidence. Renew the full local checks, seven driving regressions, five
fresh-save checks, frozen package/import/menu checks and package integrity evidence.

Remaining human coverage includes full manual-shifter and rebind testing, tactile
telemetry/impact feel, cross-vendor wheels and reconnects, complete per-track visuals,
clean-user-profile installation, a longer soak and full physical exit/relaunch
acceptance. World oscillation/normalization and New York distance artifacts/crash
remain known issues. Exotica's menu-force control defaults to the existing gate;
its gain and polarity are unchanged. Other-game controls remain post-release work.

These are accepted limits of the requested current-state release, not findings
that each wheel/game configuration is correct. Failed automated checks must be
fixed or the release held; they cannot be waived.
