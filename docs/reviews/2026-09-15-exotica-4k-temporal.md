# Current Exotica combined renderer: 4K temporal renewal

The current frozen c7e3e6b456f candidate completes5300 Amazon inputs with all9695
marked model preparations accepted, zero rejections,74010 original/private GPU
quad pairs and55794 early permissions. All3500 camera and10500 ADC rows match
the accepted full-drive prefix. The combined future, waiting and active-margin
paths remain enabled from1800; there is no abrupt debug activation in the
captured appearance interval.

There are55 consecutive completed3840×2160 CRT frames,5216–5270. Extended
presentation changes173875–238153 pixels per frame relative to original
presentation. None introduces a newly near-black pixel (all channels≤3).
The fixed car-region rectangle1450,1450–2390,2020 is identical in every frame.
This is a limited foreground check, not whole-frame semantic segmentation.
Inspected pairs5219 and5252 show additional distant forest persisting through
the tracked original fade completion, with the checked car, water and HUD intact.

Three tracked owners start original marked submissions at5219/5220 and complete
at5249/5250. Each has28 observed marked draws; their exact packed-alpha sequence,
including gaps where no draw occurs, matches the earlier original observer.
Every marked draw is admitted and prepared. Current lifetime records independently
confirm completion. The current marked-only endpoint journal does not record
the later unmarked packed248 value; that value comes from the prior observer.

Saved active-margin stages at5072 and5080 repair6459 and4231 black pixels
respectively, with zero new black pixels. Center color, entire depth and the
other page remain exact. The5219 active packet is empty and makes no change.
These are actual render-buffer comparisons during the4K run; they are not
additional completed presentation captures at5072/5080. The repaired5072 image
was inspected.

## Retained failed control and bounded reuse

The original-presentation control retains its canonical failure: its old
capture-only shutdown drain stopped at5270 before consuming the remaining GPU
tail. All5300 inputs, the requested55 completed images, source/material snapshots
and original/private saved buffers were already complete. They match the
successful candidate's inputs and corresponding saved artifacts exactly; the
control's truncated GPU journals match the successful candidate prefix.
This separately qualifies the image comparison without pretending the missing
tail passed or repeating an otherwise completed control. The
[drain correction](2026-09-15-zeus-replay-drain.md) fixes the actual failure.

Evidence: `combined-temporal4k-qualified/report.json` and
`temporal4k-handover/report.json` under the local Amazon diagnostics directory.
The successful run is `combined-temporal4k-drained-extended`; the failed control
is `combined-temporal4k-original`. No timing claim is made from paced screenshots.

This closes the targeted4K temporal and known-margin renewal for this segment.
It does not prove every turn/track, gradual appearance at the outer3× boundary,
or release-ready parity across all four games. Personal87d/publicv0.5.0 remain
unchanged. Current-resolution USA performance and remaining per-game coverage
are the next bounded acceptance work.
