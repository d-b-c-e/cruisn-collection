# Exotica 3x across a recorded race transition

The continuous combined renderer completes the original Amazon drive, scripted
name entry and loading, and the beginning of a new race at3x. This is the first
qualified continuation beyond the end of that human recording. It is useful
lifetime evidence, not acceptance of a complete second race or all courses.

`extend_input.py` retains the entire8,860-frame Amazon recording and appends
2,400 scripted frames. A separate MAME recording with current ordinary rendering
produces an11,260-frame control. All original effective inputs and emulated times
match its parent. Completed3840×2160 CRT images show name entry, loading and a
new road course with Chinese flags at11100. The appended controls are synthetic;
the new case is not labeled as another attended drive.

The same frozen native `1a1b7e0ff6b` replays that case with continuous3x
future/waiting/active scenery and marked original replacements. Its old finite
reference ends5240/5241/5242 remain unchanged. Quiet journals retain the mandatory
endpoint snapshot5219; other host/depth snapshots are omitted. The first plan
omitted that mandatory snapshot and was rejected before any emulator launch.
The failed report is retained alongside the corrected plan.

## Result

- All11,260 recorded input/time frames and native snapshots compare successfully.
  Zeus's native snapshots have the existing disabled-CPU-rendering limitation;
  the completed GL images below are the visible evidence.
- All9,460 camera records and28,380 actual ADC reads/timestamps are exact through
 11259, including the appended menus and new race start.
- All9,734 prepared scenes match their original scene boundaries/fences. Native
  summaries report ten pool epochs and32,221 prepared marked endpoint commands,
  zero endpoint rejection and296,076 GPU endpoint pairs.
- Last scene preparation11258 and mirrored frame11259 exceed the old ends.
  CPU/GPU shutdown is quiescent and joined, with zero outstanding owners/work,
  queued bytes, stream failures or writer errors. This is continuous shutdown,
  not completion of an infinite capture.
- Eight completed menu/loading images8700..10800 at300-frame intervals are
  byte-identical to ordinary rendering. The new-race image11100 changes102,002
  pixels, confined to x3502..3825/y51..666 at the far right. Viewed side by side,
  that change is foliage. The foreground car rectangle1250,1250..2550,2050 is
  exact.941 newly dark pixels are retained in the comparison, also in that
  foliage region; they are not described as absent or automatically classified
  as a missing-texture defect.

Quiet summaries are native transaction checks, not an independent saved event
history for all ten epochs. This run adds transition coverage to the earlier
independent ownership/geometry qualifications. It does not prove the outer3x
distance band contributes visibly here, remove all pop-in, or establish temporal
quality across the new course. The current new-race capture is near its start.

Local evidence under `results/diagnostics/race-transitions-20260916`:
`exotica-menu-tail-v1/case`, its `prefix-qualified.json`, the retained prelaunch
`exotica-menu-tail-3x` failure, `exotica-menu-tail-3x-v2`,
`exotica-menu-tail-3x-qualified.json`, and `exotica-menu-tail-3x-pixels.json`.
No native code changed for this qualification. Personal Stream Deck copy,
publicv0.5.0, physical FFB and hosted CI remain untouched.

Next: the same bounded transition test for V-Unit games, beginning with the
recorded Germany drive. Longer/open-course Exotica visual acceptance still
benefits from an attended recording when available.
