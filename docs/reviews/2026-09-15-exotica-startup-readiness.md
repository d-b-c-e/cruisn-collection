# Exotica guest startup readiness

The candidate can now recognize a complete, verified guest object-pool rebuild.
The saved Amazon startup reaches it at native frame 1,385, before the existing
frame-1,799 lifetime observer. This is a readiness observation, not earlier
rendering or continuous-operation acceptance.

`--exotica-bootstrap observe` requires an explicit Exotica candidate and physical
FFB disabled. It checks 18 actual instruction words, the count-write transaction,
pool extent, final sentinel write, register identity, all 1,201 links and a bounded
guest-time interval. It records the operands in a 5,012-byte proof. Invalid or
incomplete evidence does not enable anything. The observer removes its two taps
after completion; the existing lifecycle observer remains separate.

The native helper and two focused Python tests pass, including corrupted code,
links, registers, receipts and bounds. A single 2,100-input startup prefix verifies
the actual pool at base 112061. Independent analysis of its saved evidence also
matches every input/emulated-time row and original native image to the recording.

The prefix's overall combined diagnostic report **fails**: a reused fault-test
plan ends before the lifetime completion condition and contains no eligible
endpoint preparations. That failure is retained. It is not renderer acceptance.
The first offline input checker also failed because that early report exit had
not converted raw snapshots. The corrected checker converts those saved files
and passes without another game run.

Native `d002e24124f`, candidate SHA256
`dbf9611d06fdbe259badd811f3823d04246ffbbb4608de9f94d3d7bd313ec094`;
240 patches reconstruct tree `c1818510696b5eeb4631f3a2a5f138fd79078664`.
Local evidence: `exotica-amazon-20260909/bootstrap-prefix`,
`bootstrap-prefix-qualified-v2.json`, `bootstrap-native-export.json` under
`results/diagnostics`. The export script already ran; do not rerun it.

Next: use verified readiness to start lifetime tracking, qualify the additional
boot/menu transactions, then address actual scene activation and orderly exit.
Keep reset, ownership, pending-command and bounded-resource guards. The personal
installation and public v0.5.0 remain unchanged.
