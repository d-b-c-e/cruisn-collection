# Off-Road3x through car selection and another El Paso race

The continuous quiet candidate completes the original El Paso recording, car
selection and the beginning of another El Paso race. All13,044 effective inputs
and native images match, including the preserved9,644-frame attended prefix
and3,400 scripted continuation. The tail is deliberately labeled synthetic.

All11,244 camera samples and44,976 actual ADC reads/timestamps match through
13043. The renderer prepares5,426 scenes and4,688,233 quads, with geometry fold
`1ceb124602f452f5`. Last preparation13042 and presentation13043 exceed the old
finite reference end. The owned graphics worker drains4,162,277,632 ring bytes,
joins and reports no pending work, stream/GL failure or degraded preparation.

## Matched presentation and limitation

The first ordinary control inherited the attended recording's401-line native
height. The host candidate explicitly uses400. This changes pixels throughout
the image even though inputs and camera remain exact. The original report and
images remain retained; their cross-height differences are not visual acceptance.
A single additional ordinary-renderer control uses the same400-line presentation,
scale4, CRT and display as the already completed candidate. The candidate was
not rerun. The corrected comparison explicitly checks projection settings.

Of14 completed3824x2073 CRT client images9000..12900 every300 frames, ten are
exact, including the menu/car-selection views. Changed frames9000,11700,12000
and12300 contain145,045/68,673/86,835/252,420 differing pixels and454/377/173/4,904
newly dark pixels respectively. All changes stay above y838, leaving the lower
foreground exactly unchanged. The paired12300 images were inspected: additional
distant cliffs fill parts of the horizon; the truck, foreground and HUD are
unchanged. Newly dark pixels remain reported in that added distant geometry.
The12900 new-race view and11400 car-selection view were also inspected.

This is a same-course restart, approximately18 seconds into the next race at the
last screenshot. It is not an independent new-track qualification, complete
second race, continuous appearance check or3x-versus2x benefit measurement.
Known Off-Road broader-course and handover requirements remain open.

Local evidence under `results/diagnostics/race-transitions-20260916`:
`offroad-menu-tail-v1/case`, `offroad-menu-tail-3x`, retained
`offroad-menu-tail-3x-qualified.json`, matching `offroad-menu-tail-control400`,
and `offroad-menu-tail-3x-matched-qualified.json`. All use frozen native1a1b7e0ff6b;
no native code changed for this qualification. No deployment, release, physical
FFB or hosted CI occurred. Future image comparisons must match projection
settings before attributing differences to scenery.
